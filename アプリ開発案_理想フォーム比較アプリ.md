# ランニングフォーム比較アプリ 開発案
## RunGap（ラン・ギャップ）― 理想と現実の"差"を数値で見るアプリ

作成日：2026-07-16
版：v0.5
最終更新日：2026-07-27
対象：愛知工業大学 陸上競技部 長距離ブロック（競技者・指導者）

---

## 1. 企画概要

### 1.1 一言でいうと
使用者の体型を反映した3Dモデルに「理想の走り」をさせ、**実際の走行動画と比較して、どの部位がどれだけ数値的に離れているかを提示する**Webアプリ。

### 1.2 背景・課題
- ランニングフォームの改善指導は、指導者の経験則や感覚的な言葉（「もっと前傾して」「腕を大きく振れ」など）に依存しがちで、選手側は「どれくらい」直せばよいのか分からない。
- 市販のフォーム解析サービスは高価であったり、「正しいフォーム」を一方的に押し付ける設計が多く、選手個々の体型や走りの個性を無視しがちである。
- 動画を見返すだけでは、理想とのズレを定量的に把握できない。

### 1.3 設計思想（このアプリの中核）
**「正解を押し付けない計測器」であること。**

- アプリの役割は「理想の走り」と「現実の走り」の**差分を数値で提示するところまで**。
- 提示された数値をフォーム改善に取り入れるかどうか、どの指標を優先するかは、**選手・指導者本人の判断に完全に委ねる**。
- アプリは「あなたのフォームは間違っている」とは決して言わない。「理想として設定した走りと、膝関節角度が接地時に8°離れています」と事実のみを伝える。
- 評価・採点・ランク付け機能は意図的に搭載しない。

この思想は、UI文言・画面設計・通知設計のすべてに一貫して反映させる（詳細は6章）。

### 1.4 差別化ポイント
| 観点 | 一般的なフォーム解析アプリ | 本アプリ |
|---|---|---|
| 基準 | アプリ側が定義した「正しいフォーム」 | **使用者自身が選んだ「理想の走り」** |
| 体型 | 汎用モデル | **使用者の体型・ボディラインを反映した3Dモデル** |
| 出力 | 点数・評価・改善指示 | **差分の数値のみ（判断は本人に委ねる）** |
| 物理環境 | 考慮なし、または理想条件 | **現実の走行と同じ環境条件（風・傾斜等）を3Dモデルに適用** |

---

## 2. 対象ユーザーと利用シーン

### 2.1 対象ユーザー
- **主対象**：愛知工業大学 陸上競技部 長距離ブロックの競技者
- **副対象**：同ブロックの指導者（選手との対話材料として差分データを使用）

### 2.2 想定利用シーン
1. 練習日、陸上競技トラック（400m）の**ゴール付近**にスマートフォンを設置し、走行を撮影する。
2. 練習後、選手が動画をアプリにアップロードし、事前に登録しておいた「理想の走り」との比較を実行する。
3. 数分後、部位ごとの差分数値と3Dモデルの並走再生が表示される。
4. 選手は結果を確認し、必要であれば指導者と共有して次の練習方針を自分で決める。

### 2.3 利用環境の前提
- 撮影機材：スマートフォンのカメラ**1台**（三脚等での固定を推奨）
- 撮影場所：屋外・陸上競技トラック400mのゴール付近（直線区間）
- 撮影方向：初期は**側方（真横）からの撮影を標準**とする
  - 理由：矢状面（横から見た面）の指標（膝関節角度・体幹前傾角・腕振り角度・ストライド長・接地時間）は側方撮影が最も精度良く取得できるため。
  - 将来的に後方・前方撮影による左右差解析を追加検討（11章）。

### 2.4 収益モデル
- **無料**（部内ツールとして運用。サーバー費用は研究費・部費等での負担を想定）

---

## 3. コア機能一覧

| # | 機能名 | 概要 | 開発フェーズ |
|---|---|---|---|
| F1 | 体型入力・3Dモデル生成 | 身体数値の入力からボディラインを反映した3Dアバターを生成 | Phase 1 |
| F2 | 理想モーション登録 | お手本動画からモーションを抽出し、自分のアバターに適用（リターゲット） | Phase 1 |
| F3 | 環境条件入力 | 風速・気温・路面傾斜など、現実の走行環境の数値を入力 | Phase 1 |
| F4 | 走行動画アップロード・解析 | スマホで撮影した走行動画から姿勢を推定（事後解析） | Phase 1 |
| F5 | 差分比較・数値提示 | 6指標について理想と現実の差を数値で提示 | Phase 1 |
| F6 | 3D並走ビューア | 理想モデルと実走再現モデルを重ねて／並べて再生 | Phase 1 |
| F7 | 履歴管理 | 過去の解析結果を時系列で保存・閲覧 | Phase 1 |
| F8 | 指導者共有 | 解析結果を指導者アカウントと共有 | Phase 2 |
| F9 | 写真からの自動採寸 | 写真から体型データを自動取得 | Phase 3 |
| F10 | リアルタイム解析 | 走行中のリアルタイム比較表示 | Phase 3 |
| F11 | マルチプラットフォーム対応 | iOS / Androidネイティブアプリ化 | Phase 3 |

---

## 4. 機能詳細仕様

### 4.1 F1：体型入力・3Dモデル生成

**入力項目（Phase 1：手動数値入力）**
| 項目 | 単位 | 必須 | 備考 |
|---|---|---|---|
| 身長 | cm | ○ | |
| 体重 | kg | ○ | 慣性・接地衝撃の計算に使用 |
| 骨格筋率 | %（体組成計の表示値） | ○ | ボディラインの造形に反映 |
| 体脂肪率 | % | ○ | ボディラインの造形に反映 |
| 股下長 | cm | ○ | 脚長比の再現に必須 |
| 座高 | cm | △ | 未入力時は身長から統計値で推定 |
| 上腕長／前腕長 | cm | △ | 未入力時は身長から統計値で推定 |
| 肩幅 | cm | △ | 同上 |
| 足長（足のサイズ） | cm | △ | 接地判定の補助に使用 |

**3Dモデル生成の仕組み**
- ベースとなるパラメトリック人体モデル（SMPL系のボディモデル、またはUnity上のブレンドシェイプ付き汎用素体）を用意し、入力値から体格パラメータ（各部位の長さ・太さ）を算出して変形する。
- 骨格筋率が高いほど大腿・下腿・肩周りのボリュームを増し、体脂肪率が高いほど胴回り・四肢の丸みを増す、という写像ルールで**使用者のボディラインを近似的に再現**する。
- 生成されたモデルはその場で360°回転して確認でき、「自分の体に見えるか」を使用者が目視チェックしてから確定する。
- ※体組成の数値は外見の完全な再現を保証するものではないため、確認画面に「体型の近似モデルです」と明記する。

**Phase 3拡張**：正面・側面の2枚の写真からシルエットを抽出し、体格パラメータを自動推定する自動採寸機能を追加。

### 4.2 F2：理想モーション登録

**方式**：お手本となる走行動画（プロ選手の映像、チームメイトの走り、過去の自分のベスト時の走りなど）を読み込み、そのモーションを抽出して**使用者の体型のアバターに適用（モーションリターゲティング）**する。

**処理フロー**
1. 使用者がお手本動画をアップロードする（側方撮影のものを推奨。推奨条件を画面に明示）。
2. サーバー側で3D姿勢推定（後述7.3）を行い、関節ごとの時系列モーションデータを抽出する。
3. 抽出したモーションを使用者の体型（脚長・腕長など）に合わせてリターゲットする。**このとき関節「角度」を保存し、位置ではなく角度ベースで転写する**ことで、体格差による不自然な変形を防ぐ。
4. リターゲット後のモーションで走る自分のアバターをプレビュー表示し、使用者が「これを理想として登録する」を押して確定する。
5. 理想モーションは複数登録でき、名前を付けて管理できる（例：「〇〇選手ラスト400m」「自己ベスト時の走り」）。

**重要な注意表示**：お手本が他者である場合、体格差により物理的に再現不可能な動きになる可能性がある。リターゲット時に物理的整合性チェック（後述7.4）を行い、破綻がある場合は警告を表示する。

**著作権・肖像権への配慮**：アップロードされたお手本動画は本人の私的解析目的にのみ使用され、他ユーザーへ公開されないことを利用規約とUI上に明記する。テレビ放送等の映像を扱う場合の法的リスクについての注意喚起も表示する。

### 4.3 F3：環境条件入力（現実準拠の物理パラメータ）

理想モーションでアバターを走らせる際、「現実で走る時と変わらない環境」を再現するため、以下の数値を入力する。**入力が簡単であることを最優先**とし、必須項目を最小限に絞る。

| 項目 | 単位 | 入力方法 | 既定値 | シミュレーションへの反映 |
|---|---|---|---|---|
| 風速 | km/h | 数値入力（大きさのみ・符号なし） | 0 | 風ベクトルの大きさ。空気抵抗力の算出に使用 |
| 風向 | ホームストレートでの向き（追い風／向かい風／横風・右／横風・左） | 選択式 | 追い風 | 風をトラックに対する固定ベクトルとして設定し、周回中の各区間（両直線・両コーナー）に反映 |
| 気温 | ℃ | 数値入力 | 20 | 空気密度の補正（軽微） |
| 路面傾斜 | % | 数値入力 | 0（トラックは平坦） | 重力の分力 |
| 路面種別 | タータン／土／アスファルト | 選択式 | タータン | 反発係数・摩擦係数のプリセット適用 |
| 走行速度（目標ペース） | 分:秒/km または m/s | 数値入力 | ─（必須） | アバターの走行速度 |

- 重力加速度（9.81 m/s²）、空気密度の基準値、使用者の体重・慣性はアプリ側で自動設定し、使用者には入力させない。
- **風の扱い（周回モデル前提）**：本アプリはアバターを400mトラック上で**周回走行**させる想定のため、風は「追い風か向かい風か」の一方に定まらない。1周する間に、同じ風でもホームストレートで追い風なら向かい側の直線では向かい風となり、コーナーでは横風成分を受ける。そこで風は**トラックに対して固定された1本のベクトル**として扱い、次の2値で一意に指定する。
  - **風速**：ベクトルの大きさ（km/h、符号なし）。「追い風＋／向かい風−」のような符号は用いない。
  - **風向**：そのベクトルの向きを**ホームストレートを基準に**選択する（ホームストレートが「追い風／向かい風／横風・右／横風・左」のいずれか）。ホームストレートの向きを決めれば、他の直線・コーナーで受ける風向きはトラック形状から自動的に定まる。
  - この設計により、風速と風向で表す情報が重複しない（大きさと向きを分担して指定する）。
- 愛知工業大学の利用が主であるため、「いつものトラック」プリセット（タータン・傾斜0%）を初期選択状態にし、通常は**風（風速・風向）とペースだけ入力すれば開始できる**UIとする。

### 4.4 F4：走行動画アップロード・解析（事後解析型）

**撮影ガイド機能**
- アップロード画面に撮影方法の図解を常設する：
  - スマホを地面から約1.0〜1.5mの高さで三脚固定
  - 走路から10〜15m離れた真横の位置（ゴール付近の外側）
  - 選手が画面を横切るように撮影（最低2〜3歩、推奨5歩以上が映ること）
  - 60fps以上での撮影を推奨（接地時間の分解能向上のため。30fpsでも解析可能だが精度低下を明示）
- 動画アップロード時に自動チェックを行い、fps不足・被写体が小さすぎる・ブレが大きい場合は警告を出す（解析自体は続行可能）。

**解析処理**
1. 動画をサーバーへアップロード（対応形式：mp4 / mov、最大60秒・500MB目安）。
2. サーバー側で3D姿勢推定を実行し、関節位置・角度の時系列データを生成。
3. 歩行周期（接地〜離地〜次の接地）を自動検出し、周期ごとに指標を算出。
4. 解析完了までの目安時間（例：約2〜3分）と進捗バーを表示。完了時は画面内通知。

- **絶対スケール（cm）の較正について**：単眼映像そのものには絶対的な長さの情報が含まれないため、ストライド長などをcmで出力するには基準となる実寸が必要である。本アプリでは、F1で入力済みの**使用者の身体寸法（身長・股下長など）を推定された3D骨格に当てはめて画素↔実長の換算係数（スケール）を求め**、これを基準に距離系指標（ストライド長・接地位置）をcmへ換算する。したがってストライド長等のcm精度は、体型入力値の正確さと姿勢推定の骨格スケール精度に依存する。

### 4.5 F5：差分比較・数値提示（本アプリの核心機能）

**比較の枠組み**
- 理想モーション・実走モーションの双方を**1歩行周期（接地→次の同側接地）で正規化**し、周期内の対応する局面（接地瞬間・立脚中期・離地瞬間・遊脚中期）同士で比較する。
- 走行速度が異なると指標が変わるため、**比較は同一目標ペース条件で行う**（理想モーション側をF3の入力ペースで再生した状態と比較）。ペースが大きく異なる場合は画面上に注意を表示する。

**6つの比較指標と提示方法**

| 指標 | 差分の単位 | 測定定義 | 提示方法 |
|---|---|---|---|
| 膝関節角度 | 度（°） | 大腿と下腿のなす角。接地時・立脚中期・離地時・遊脚最大屈曲時の4局面で比較 | 局面別の角度差＋周期全体の角度カーブ重ね描き |
| 体幹前傾角 | 度（°） | 鉛直線に対する体幹（肩−腰を結ぶ線）の前傾角。周期平均と接地時で比較 | 平均差＋時系列カーブ |
| 腕振り角度 | 度（°） | 肩関節の前後方向の振り幅（最前点と最後点の角度範囲） | 振り幅の差＋前方向・後方向それぞれの差 |
| 接地時間 | ミリ秒（ms） | 足が地面に接している時間（接地〜離地）。左右それぞれ算出 | 差分ms＋左右それぞれの値（※左右差の注意は下記参照） |
| ストライド長 | センチメートル（cm） | 同側接地から次の同側接地までの距離の1/2（1歩の距離） | 差分cm |
| ピッチ | 歩/分（spm） | 1分間あたりの歩数 | 差分spm |

**数値提示のルール（設計思想の反映）**
- 各指標は「理想値／実測値／差分」の3つを**中立的な言葉**で並べる。
  - 例：「膝関節角度（接地時）　理想 155° ／ あなたの走り 148° ／ 差 −7°」
- 差の大小に対する良し悪しの評価語（「悪い」「要改善」「不合格」など）は**一切使わない**。
- 差が大きい指標を視覚的に目立たせる場合も、赤色の警告表現ではなく、**濃淡や差分バーの長さ**で「差の大きさ」だけを表現する。
- 各指標名の横に「？」アイコンを置き、タップすると「この指標が何を意味するか」の中立的な解説を表示する（改善方法の指示はしない）。

**接地時間の左右計測に関する注意（側方単眼撮影の制約）**
- 真横から1台で撮影する構成では、カメラに近い側の脚が奥側の脚を**遮蔽（オクルージョン）**するため、奥側脚の接地・離地タイミングは手前側脚に比べて推定が不安定になりやすい。
- そのため接地時間の左右値は**単一の1歩から確定せず、映像内の複数歩（同側の複数周期）から統計的に推定**し、可能な範囲で左右の脚が手前に来る局面をそれぞれ利用して精度を補う。
- 上記の理由により、**接地時間の左右値は左右で信頼度が異なる場合がある**。各値には他指標と同様に推定信頼度（高・中・低）を添え、奥側脚など遮蔽の影響が大きいと判断される値には低めの信頼度を表示する。左右差そのものを重視する用途では、信頼度表示を必ず併せて確認する旨を指標解説（「？」）にも明記する。
- 左右差を主目的とする計測には、将来的な後方・前方カメラの追加（11章）がより適することを併記する。

### 4.6 F6：3D並走ビューア

- 理想モーションのアバター（半透明の青系）と、実走から再現したアバター（実体の橙系）を、
  - **重ね表示モード**（同一位置に重ねてズレを直接見る）
  - **並走表示モード**（横に並べて再生する）
  の2モードで再生できる。
- 再生コントロール：再生／一時停止、0.1〜1.0倍速のスロー再生、1フレーム送り、任意局面（接地・離地など）へのジャンプボタン。
- カメラは自由回転・ズーム可能。「側面」「正面」「上面」のプリセット視点ボタンを用意する。
- 現在表示中のフレームにおける各指標の瞬時値を画面下部に小さく表示する。

### 4.7 F7：履歴管理

- 解析結果は日付・使用した理想モーション名・環境条件とともに自動保存される。
- 指標ごとの差分の推移を折れ線グラフで表示できる（例：「接地時間の差」が練習を重ねるにつれどう変化したか）。
- グラフにも評価語は付けない。変化の解釈は本人に委ねる。
- 各解析結果にメモ（自由記述）を残せる（例：「今日はピッチを意識した」）。

---

## 5. 画面構成・画面遷移

```
[ログイン]
   │
[ホーム（ダッシュボード）]
   ├─ [マイモデル]           … 体型入力・3Dモデル確認/編集
   ├─ [理想モーション一覧]    … 登録済み理想の管理
   │     └─ [理想モーション登録]（お手本動画アップロード → プレビュー → 確定）
   ├─ [新規解析]
   │     ├─ Step1: 走行動画アップロード（撮影ガイド常設）
   │     ├─ Step2: 比較する理想モーションの選択
   │     ├─ Step3: 環境条件入力（プリセット初期値済み）
   │     └─ Step4: 解析実行 → 進捗表示 → [結果画面]
   ├─ [結果画面]
   │     ├─ 差分サマリー（6指標の一覧カード）
   │     ├─ 3D並走ビューア
   │     └─ 指標別詳細（カーブ重ね描き・局面別差分）
   ├─ [履歴]                 … 過去結果一覧・推移グラフ
   └─ [設定]                 … アカウント・データ削除など
```

---

## 6. UI/UX設計方針

1. **「4ステップで解析開始」を厳守する**
   新規解析は「動画を選ぶ → 理想を選ぶ → 環境を確認する → 実行」の4ステップに収め、各ステップの画面には1つの主要操作しか置かない。練習後の疲れた状態でも迷わず操作できることを目標とする。

2. **入力の負担を最小化する**
   - 環境条件はプリセット＋前回値の記憶により、通常は風（風速・風向）とペースのみの入力とする。風向はホームストレート基準の選択式1タップで指定できるようにする。
   - 体型入力は初回のみ。必須5項目・任意4項目に分け、任意項目は「あとで入力」を許す。

3. **中立的な言葉づかい（設計思想のUIへの落とし込み）**
   - 「診断」「判定」「スコア」「改善点」という語を全画面で使用禁止とし、「比較」「差分」「数値」「記録」という語に統一する。
   - 差分の色表現は警告色（赤）を避け、単一色の濃淡で差の大きさのみを表す。

4. **スマートフォンでの閲覧を第一に設計する（レスポンシブ）**
   トラックサイドでスマホから結果を見る利用が主となるため、モバイル幅を基準にレイアウトし、3Dビューアもタッチジェスチャー（1本指回転・2本指ズーム）に対応させる。

5. **待ち時間の体験設計**
   解析中は進捗バーと残り時間目安を表示し、完了までの間に「今回の環境条件の確認」「過去の結果の閲覧」ができるようにして待ち時間の不満を軽減する。

6. **精度の限界を隠さない**
   単眼カメラによる推定には誤差がある。各指標に推定信頼度（高・中・低）を小さく添え、信頼度が低い場合はその旨を表示する。誤った数値を断定的に見せないことも「計測器」としての誠実さの一部とする。

7. **アクセシビリティ**
   色だけに頼らない情報表現（数値・ラベル併記）、十分なコントラスト比、フォントサイズ変更対応。

---

## 7. 技術構成

### 7.1 全体アーキテクチャ（Phase 1：Webアプリ）

```
[ブラウザ（PC/スマホ）]
  ├─ フロントエンド：Unity WebGLビルド（3D表示部）＋ HTML/JS（フォーム・一覧部）
  │    ※3Dビューア・アバター表示をUnityで実装しWebGLとして埋め込む
  ├─ 動画アップロード ──→ [アプリケーションサーバー]
  │                          ├─ API（アカウント・データ管理）
  │                          ├─ 解析ジョブキュー
  │                          └─ [解析ワーカー（GPUサーバー）]
  │                                ├─ 3D姿勢推定（機械学習モデル）
  │                                ├─ 歩行周期検出・指標算出
  │                                └─ モーションリターゲット処理
  └─ 結果データ（JSON＋モーションデータ）を受信して表示
[データベース]：ユーザー・体型・理想モーション・解析結果
[ストレージ]：動画ファイル（一定期間後の自動削除ポリシーを設定）
```

### 7.2 解析処理の場所：**サーバー処理を推奨（Phase 1）**

| 観点 | 端末内（ブラウザ内）処理 | サーバー処理（推奨） |
|---|---|---|
| 精度 | ブラウザで動く軽量モデルに限られ、3D推定精度が低い | 高精度な3D姿勢推定モデル（研究レベルのモデル）を使用可能 |
| 端末差 | 使用者のスマホ性能により速度・成否がばらつく | どの端末からでも同じ品質・同じ速度 |
| 開発難度 | WebGL/WASMへのモデル移植が必要で難度が高い | Python等の標準的な機械学習環境で実装でき、開発・改良が容易 |
| コスト | サーバー費用ほぼ不要 | GPUサーバー費用が発生。ただし対象が1チーム規模のため小さい |
| リアルタイム性 | 有利 | 事後解析なら問題なし |

**結論**：Phase 1は事後解析型であり、精度・開発容易性・端末非依存性を優先して**サーバー処理**とする。Phase 3のリアルタイム型では、粗いリアルタイム表示を端末内の軽量モデルで行い、精密解析はサーバーで行う**ハイブリッド構成**へ発展させる。

**サーバーの調達方針（確定）**：運用サーバーには**大学研究室のGPUサーバー（オンプレミス）**を用いる。クラウドGPUは採用しない。理由は、体組成データ・動画といった機微情報を**学外に出さずに扱える**ためプライバシー・同意面の負担が小さいこと、1チーム・事後解析・低頻度という負荷特性に対して研究室のGPU1台で十分成立することによる。ただし**開発の初期段階はローカルサーバー（開発機）で構築・検証**し、動作が固まった段階で研究室のGPUサーバーへ移行する。運用開始時は**初期利用者を2〜3名に限定**し、動作・精度・運用フローを確認してから段階的に対象を広げる。

### 7.3 主要技術候補

| 層 | 技術候補 | 備考 |
|---|---|---|
| 3D表示 | **Unity（WebGLビルド）** | 指定どおり。アバター表示・並走ビューア・モーション再生を担当 |
| 人体モデル | SMPL系パラメトリックモデル or ブレンドシェイプ付き素体 | 体型パラメータからのボディライン変形に対応 |
| 3D姿勢推定 | 単眼動画→3D関節。次の4系統を候補とする：①2D検出（RTMPose／MMPose系）、②高精度2D（ViTPose系）を用いた2D→3D積み上げ、③人体メッシュ復元型（SMPL系・CameraHMR等）、④拡散・フローマッチング系（FMPose3D等） | サーバー側GPUで実行。①②は「2D→3D積み上げ型」、③は身体寸法との相性が良くバイオメカニクス向き、④は近年の高精度手法。Phase 0でmocopi（IMU）とGarmin/Coros（ランニングダイナミクス）の参照値と突き合わせて誤差比較し選定 |
| フロントエンド | HTML/CSS/JavaScript（フレームワークはReact等を検討） | フォーム・一覧・グラフ表示 |
| バックエンド | Python（FastAPI等）＋ジョブキュー | 姿勢推定と同じ言語圏で保守しやすい |
| データベース | PostgreSQL 等 | |
| 物理シミュレーション | Unity物理エンジン＋独自の走行力学計算 | 環境条件（風・傾斜・路面）の反映 |

### 7.4 物理的整合性チェック（リターゲット時）
- リターゲット後のモーションに対し、接地の貫通・浮遊、関節可動域超過、目標ペースとストライド×ピッチの不整合を自動検査する。
- 破綻が検出された場合は自動補正を試み、補正した箇所を使用者に通知する（黙って改変しない）。

---

## 8. データモデル（主要エンティティ）

```
User（使用者）
 ├─ id, 氏名, 役割（選手/指導者）, 所属
 ├─ BodyProfile（体型プロファイル）※履歴保持（体型は変化するため）
 │    └─ 身長, 体重, 骨格筋率, 体脂肪率, 股下長, 座高, 上腕長, 肩幅, 足長, 記録日
 ├─ IdealMotion（理想モーション）[複数]
 │    └─ 名称, 元動画メタ情報, モーションデータ(関節角度時系列), 登録日
 └─ AnalysisResult（解析結果）[複数]
      ├─ 走行動画メタ情報, 使用した理想モーションid, 使用した体型プロファイルid
      ├─ EnvironmentCondition（風速, 風向, 気温, 傾斜, 路面, ペース）
      ├─ MetricComparison × 6（指標名, 理想値, 実測値, 差分, 信頼度, 局面別内訳）
      ├─ 実走モーションデータ（3D再現用）
      └─ メモ, 作成日
```

- **プライバシー**：体組成・動画は個人の機微情報として扱い、本人と本人が共有を許可した指導者のみ閲覧可能とする。動画原本は解析後一定期間（例：30日）で自動削除し、モーションデータのみ保持する設計を既定とする。

---

## 9. 開発フェーズ計画

### Phase 0：プロトタイプ検証（約2〜3ヶ月）
- 目的：**単眼スマホ動画からの姿勢推定精度が、6指標の比較に耐えるかの検証**（本企画最大のリスクの検証）
- 内容：候補とする姿勢推定モデル（4系統：①RTMPose／MMPose系、②ViTPose系、③人体メッシュ復元型〔SMPL系・CameraHMR等〕、④拡散・フローマッチング系〔FMPose3D等〕）を実際のトラック撮影動画で比較評価する。精度の基準（参照値）として、関節角度系（膝関節角度・体幹前傾角・腕振り角度）はSony mocopi（6点IMUによるモバイルモーションキャプチャ）、時間・距離系および左右差（接地時間・ピッチ・ストライド長・接地時間の左右バランス）はGarmin/Corosのランニングダイナミクス（接地時間・左右バランスの取得には胸ストラップまたはランニングダイナミクスポッドが必要）を用いる。動画・mocopi・ウォッチを同時計測し、開始時の同期マーカー等で時刻を合わせたうえで各指標を突き合わせる。ただしmocopi・ウォッチはいずれも推定値であり厳密な真値（ゴールドスタンダード）ではないため、Phase 0は「真値との誤差測定」ではなく「動画推定・mocopi・ウォッチという複数の独立した推定の一致度検証」と位置づけ、動画推定が両参照値から大きく外れる指標を要注意として抽出する。Garmin/Corosの接地時間は秒単位・平均寄りの粒度のため、1歩ごとでなく一定区間の平均で比較する。
- 判断基準の例：膝関節角度誤差±5°以内、接地時間誤差±20ms以内（達成できない場合は指標の絞り込み・撮影条件の厳格化を検討）

### Phase 1：Webアプリ初版（約4〜6ヶ月）
- F1〜F7を実装（事後解析型・手動体型入力・Web）
- 開発初期はローカルサーバー（開発機）で構築・検証し、動作が固まり次第、研究室のGPUサーバーへ移行する。
- 運用開始時は初期利用者を2〜3名に限定して試験運用し、フィードバックを反映しながら段階的に対象を拡大する。

### Phase 2：チーム運用機能（約2〜3ヶ月）
- F8（指導者共有）、履歴グラフの拡充、運用改善

### Phase 3：拡張（時期未定）
- F9（写真自動採寸）、F10（リアルタイム型：端末内軽量推定＋サーバー精密解析のハイブリッド）、F11（iOS/Androidネイティブ対応）

---

## 10. リスクと対応方針

| リスク | 影響 | 対応 |
|---|---|---|
| 単眼推定の精度不足 | 差分数値の信頼性が崩れ、アプリの存在意義に関わる | Phase 0で最初に検証。信頼度表示で限界を明示。撮影ガイドで条件を統制 |
| 屋外撮影条件のばらつき（逆光・雨・他選手の映り込み） | 解析失敗・精度低下 | アップロード時の自動品質チェックと再撮影の案内。複数人が映る場合の対象選択UI |
| 体格差のあるお手本のリターゲット破綻 | 不自然な「理想」が生成される | 角度ベース転写＋物理的整合性チェック＋警告表示（7.4） |
| Unity WebGLのスマホブラウザでの動作重量 | モバイルでの3D表示が重い | ポリゴン数・テクスチャの最適化。低スペック向け簡易表示モード |
| 数値の独り歩き（差分＝悪と誤解される） | 設計思想の毀損、選手の混乱 | UI文言規則（6章3項）、指標解説、指導者向け利用ガイドの整備 |
| 体組成・動画のプライバシー | 信頼問題 | 8章のアクセス制御・自動削除ポリシー、同意取得フロー |

---

## 11. 将来検討事項（メモ）
- 後方・前方カメラ追加による左右差（左右対称性）指標の追加
- 複数の理想モーションとの同時比較
- 大会映像（ゴール付近固定カメラ）との連携
- 疲労局面（ラスト400m）専用の比較プリセット

---

## 12. 未確定事項（要決定）
- 利用規約・同意文面の最終確定：ドラフト作成済み（別紙「利用規約_同意文面_ドラフト.md」）。大学の個人情報保護・研究倫理の窓口および必要に応じ専門家の確認を受けて確定する。

**確定済みの事項（旧・未確定事項からの決定）**
- アプリの正式名称：**RunGap** に確定。
- サーバーの調達方法：**大学研究室のGPUサーバー（オンプレミス）**を採用（クラウドGPUは不採用）。開発初期はローカルサーバーで構築・検証し、研究室サーバーへ移行。運用開始時は初期利用者2〜3名に限定（7.2・Phase 1参照）。
- Phase 0の姿勢推定モデル候補：4系統を列挙済み（7.3・Phase 0参照）。最終選定はPhase 0で実施。
- Phase 0の評価環境（精度の基準）：**Sony mocopi（関節角度）＋ Garmin/Coros のランニングダイナミクス（接地時間・ピッチ・ストライド長・接地時間の左右バランス）**を参照値として用いることに確定（光学式モーションキャプチャは不要）。両者とも推定値であり真値ではないため、Phase 0は「複数の独立した推定の一致度検証」として運用する（7.3・9章Phase 0参照）。同時計測・時刻同期の実施はPhase 0の作業内で行う。
