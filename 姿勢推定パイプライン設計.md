# RunGap 姿勢推定パイプライン・モデルアダプタ設計

版：v0.10（企画段階）／作成日：2026-07-28
対象プロジェクト：ランニングフォーム比較アプリ RunGap
対象読者：バックエンド／コンピュータビジョン開発メンバー（利用者向けの文書ではありません）

> 本書は「データモデル・API 詳細設計」を前提に、姿勢推定を**差し替え可能**にした
> 共通パイプラインと、初期2モデル（軽量2D＋メッシュ復元）の統合方針を定めます。
> Phase 0 の一致度検証を、使い捨ての実験ではなく**「評価モード」としてコードに
> 取り込む**方針です。パイプラインは採点・評価を一切出力せず、算出するのは
> 「理想値／実測値／差／信頼度／局面別内訳」までとし、中立性を保ちます。

---

## 1. 設計方針

- **モデル非依存の共通パイプライン** ＋ 差し替え可能な `PoseEstimator` アダプタ。
- **初期2モデル**：A＝軽量2D（RTMPose/MMPose → 2D→3D リフト）、B＝メッシュ復元（SMPL系・CameraHMR）。あとから ②ViTPose・④拡散/フローマッチング系を追加できる設計。
- **共通スケルトンへ正規化**して、下流（検出・較正・指標算出）を完全にモデル非依存にする。
- **評価モード**で参照（mocopi／ウォッチ）との一致度を出力する（＝ Phase 0 をコード化）。
- **中立性**：パイプラインは score/rank/grade/評価ラベルを持たない。

---

## 2. 全体パイプライン（段階）

1. **前処理**：フレーム抽出、fps 確認、人物検出・トラッキング（複数人が映る場合は対象選択）。
2. **姿勢推定（アダプタ）**：選択したモデルでモデル固有の3D姿勢（またはメッシュ）を得る。
3. **正規化**：モデル固有出力を**共通スケルトン**（後述）へマッピング。
4. **cm較正**：体型寸法から画素↔実長のスケールを確定（軽量2Dで特に重要）。
5. **歩行周期検出**：接地・離地イベントを検出し、局面（接地／立脚中期／離地／遊脚）に分割。
6. **6指標算出**：局面別・左右で算出。
7. **信頼度付与**：指標×条件ごとに high/medium/low。
8. **出力**：`metric_comparisons` と `run_motion_data`（データモデル設計に一致）。

---

## 3. 共通インターフェース（`PoseEstimator`）

擬似コード（Python）。実装言語・FWは実装時に確定するが、境界はこの形を守る。

```python
@dataclass
class FrameSeq:
    frames: list          # 画像列（またはデコーダ参照）
    fps: float

@dataclass
class CameraSpec:
    view: str = "side"    # 側方前提
    height_m: float | None = None

@dataclass
class BodySpec:            # body_profile 由来
    height_cm: float
    inseam_cm: float
    weight_kg: float
    segments: dict         # 任意寸法（上腕/前腕/肩幅/足長 等、未指定は推定）

@dataclass
class Pose3DSeq:
    joints: "np.ndarray"   # [T, J, 3] 共通スケルトン順
    per_joint_conf: "np.ndarray"  # [T, J] 推定信頼度
    metric_scale: bool     # 出力がメートルスケールか
    mesh: object | None = None    # メッシュ復元系のみ

class PoseEstimator(ABC):
    name: str                      # analysis_jobs.model_variant と対応
    provides_mesh: bool = False
    has_metric_scale: bool = False
    @abstractmethod
    def estimate(self, frames: FrameSeq, camera: CameraSpec, body: BodySpec) -> Pose3DSeq: ...

# レジストリ：model_variant 名でアダプタを解決
REGISTRY: dict[str, type[PoseEstimator]] = {}
def register(cls): REGISTRY[cls.name] = cls; return cls
```

- ワーカーは `analysis_jobs.model_variant` からアダプタを解決して実行する。
- **下流は `Pose3DSeq`（共通スケルトン）だけを見る**ため、モデルを足しても下流は不変。

---

## 4. 共通スケルトン（正規化スキーマ）

- 指標算出に必要な関節を定義し、全モデルの出力をこの並び・定義へ写像する。
  - 頭部、頸、体幹（胸郭・骨盤）、左右の肩・肘・手首、左右の股関節・膝・足首、左右のつま先・踵。
- **左右の識別**、座標系（側方カメラ基準：進行方向x・鉛直y・奥行きz）、単位（メートル）を固定。
- 各アダプタは「モデル固有関節 → 共通スケルトン」のマッピングを持つ（不足関節は補間・推定）。
- この層があることで、指標算出・cm較正・歩行検出のコードは**一度書けば全モデルで再利用**できる。

---

## 5. 初期2モデルのアダプタ

### 5.1 A：軽量2D（RTMPose / MMPose → 3D リフト）
- 流れ：2Dキーポイント検出 → 3Dリフト → 共通スケルトンへ。
- 特性：**軽い・速い**。ただし **スケール不定**（→ cm較正が必須）。奥行き・左右の曖昧さがあり、**接地時間の左右差は低信頼度になりやすい**。
- フラグ：`has_metric_scale=False`, `provides_mesh=False`。

### 5.2 B：メッシュ復元（SMPL系・CameraHMR）
- 流れ：画像 → 人体メッシュ（形状β＋姿勢θ）→ 関節 → 共通スケルトンへ。
- 特性：**メートルスケールの人体**が出るため、**cm較正・体節長と相性が良い**。形状β に体型寸法（身長・股下等）を反映できる。処理は重い。
- フラグ：`has_metric_scale=True`, `provides_mesh=True`。メッシュは `run_motion_data`（3D表示）にも活用。

> 2本とも最終的には `Pose3DSeq` に落ちるので、下流は共通。違いは「較正の重み」と「信頼度の初期値」に主に現れる。

---

## 6. cm較正

- `BodySpec`（身長・股下長ほか）を推定骨格に当て、画素↔実長のスケールを決定。
- A（スケール不定）では必須。B では寸法が近いため**補正主体**。
- ストライド長・接地時間の距離換算などスケール依存の指標に効く。

---

## 7. 歩行周期検出・局面分割

- 足首・つま先・踵の上下動と速度から接地／離地イベントを検出し、接地／立脚中期／離地／遊脚に分割。
- **側方単眼の遮蔽**：手前脚が奥脚を隠すため、奥側のイベントは不安定 → 左右で信頼度に差が出る前提で扱う。

---

## 8. 6指標算出（モデル非依存）

| 指標 | 算出定義（要点） | 単位 | 左右 |
|---|---|---|---|
| 膝関節角度 | 接地局面の膝屈曲角 | deg | none |
| 体幹前傾角 | 1周期の平均前傾 | deg | none |
| 腕振り角度 | 前後の振り幅 | deg | none |
| 接地時間 | 接地〜離地 | ms | left/right |
| ストライド長 | 1歩の距離（較正後） | cm | none |
| ピッチ | 歩数/分 | spm | none |

- 出力は `metric_comparisons`（理想／実測／差／単位／信頼度／局面別）に一致させる。

---

## 9. 信頼度の付与

- **一次（ヒューリスティック）**：各フレームの推定信頼度、遮蔽の有無、fps、検出の安定性から high/medium/low を仮決め。
- **二次（キャリブレーション）**：評価モードの一致度結果を使い、**指標×条件ごとに信頼度の閾値を較正**する。これにより信頼度表示が"根拠のある値"になる（＝精度の把握が製品機能に直結する）。

---

## 10. 評価モード（Phase 0 をコード化）

- 入力：同一試技の**参照データ**（mocopi の関節角度／ウォッチの接地時間・左右バランス）と、手拍子による時刻同期情報。
- 処理：本パイプラインの各モデル出力と参照を、区間平均で突き合わせ。
- 出力：**一致度テーブル**（指標×モデル×差×判定）。Phase 0 手順書の記録様式と対応。
- 位置づけ：CLI／管理用ツールとしてプロダクト API から分離。**評価用に複数モデルを走らせ**、プロダクトは選定した1モデルで動かす。

---

## 11. データモデル・API との接続

- `analysis_jobs.model_variant` ＝ 使用アダプタ名。ワーカーが本パイプラインを実行し、`metric_comparisons`・`run_motion_data` を書き込む（データモデル設計のジョブフロー通り）。
- 評価時は「試技×モデル」で複数の結果を保持し得るが、**プロダクト提供は選定1モデル**。この使い分けを実装で明示する。

---

## 12. モジュール構成（例）

```
pipeline/        # 段階のオーケストレーション
estimators/      # base.py, rtmpose_adapter.py, meshrecovery_adapter.py, registry.py
skeleton/        # canonical.py（共通スケルトン定義・マッピング）
calibration/     # scale.py（cm較正）
gait/            # events.py（接地/離地）, phases.py（局面）
metrics/         # 6指標の算出
confidence/      # 信頼度（一次・キャリブレーション）
evaluation/      # 参照取り込み・同期・一致度
io/              # ストレージ・動画デコード
```

---

## 13. 中立性・プライバシーの担保（再掲）

- パイプラインは評価・採点を出力しない。差・信頼度・局面別のみ。
- 動画はワーカー処理後、原本を30日で自動削除（データモデル設計）。モーション・数値は保持。

---

## 14. 実装の進め方（初期2本・段階）

1. **共通スケルトン ＋ `PoseEstimator` IF ＋ ダミーアダプタ**でパイプラインを端から端まで通す（1本の実データで結果が出るところまで）。← 最短で"動く土台"。
2. **A：軽量2Dアダプタ**を実装。
3. **B：メッシュ復元アダプタ**を実装。
4. **評価モード**で A・B の一致度を出す。
5. **cm較正・信頼度キャリブレーション**を詰める。

> この順なら、UI・本番DB・3Dビューアの磨き込みといった上位レイヤーに手を広げる前に、
> 「数値が信頼できるか」の当たりを付けられる。土台（1〜3）はどのモデルが最終選定されても
> 再利用でき、無駄にならない。
